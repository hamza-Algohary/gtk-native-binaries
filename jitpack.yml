jdk:
   - openjdk11
before_install:
   - make build
install:
   - mvn install:install-file -Dfile=lib/gtk_native.jar -DgroupId=$GROUP -DartifactId=$ARTIFACT -Dversion=$VERSION -Dpackaging=jar -DgeneratePom=true